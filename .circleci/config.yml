jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    steps:
      - checkout
      - run:
          name: "vu"
          command: |
            echo $MY_ENV_VAR
workflows:
  test-env-vars:
    jobs:
      - build